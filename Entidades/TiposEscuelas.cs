namespace CoreEscuela.Entidades
{
    public enum TiposEscuela
    // los enum nos sirven para limitar opciones para una propieddad y estos se enumeran desde el 0 como los vas escribiendo y esto minimiza los errores en el desarollo 
    {
        Primaria, Secundaria, PreEscolar
    }
}